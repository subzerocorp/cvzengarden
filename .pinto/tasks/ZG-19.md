+++
id = "ZG-19"
title = "Put the print-honesty rule, a full blank scaffold, and contract fixes where designers look"
status = "todo"
rank = "zi"
labels = ["avril", "round-1", "designer", "docs", "avril-blessed"]
depends_on = ["ZG-16", "ZG-17", "ZG-14"]
created = "2026-08-23T23:57:34.104718Z"
updated = "2026-08-24T18:25:02.049541Z"
+++

## Why
Devon shipped four pages of black ink because the paper rule lives only in the QA bar, Mika retyped the §6 inventory into `_blank.css` by hand, the contract snippets/font rules contradict each other across four files, and the byte-locked sample never shows `rz-photo` / `rz-score`, so a Designer cannot proof them (Devon, Mika).
## Covers
C6, C8 (§5.10 snippet, font allowlist wording, `rz-photo` / `rz-score` proofing fixture), C7 (doc contradictions) — Devon, Mika
## Scope in
- `skeleton/CLASS-CONTRACT.md` §2 target table: replace "do not fight `@media print` unless you mean to" with the rule: "Whatever you do on screen, print must be dark ink on white paper with motion off — a hiring manager prints this. `web` themes inherit the Skeleton's readable print fallback; if you override `@media print` it must stay paper-honest (BAR-U3)."; same sentence in `themes/README.md`
- `_blank.css`: every §6 selector present and empty, grouped like §6, with an `@media print { }` block carrying the rule comment, a `@media (prefers-reduced-motion: reduce) { }` block, and the header fields as left by ZG-16 (`Name`, `Author`, `URL`, `License`, `rz-target`) and ZG-17 (license wording) — this PBI edits the header last, hence the dependencies
- §6 templated tokens are expanded, not skipped: `rz-contact--{type}` → `email`, `phone`, `location`, `url`, `other` (§5.2 contact types); `rz-link--{type}` → `website`, `github`, `gitlab`, `linkedin`, `mastodon`, `bluesky`, `dribbble`, `behance`, `twitter`, `x`, `other` (§5.2 profile modifiers); `rz-section--…` variants are already literal in §6. The node test defines the same expansion table
- §5.10 snippet shows the `<ul class="rz-skill-groups">` wrapper; §2 fonts sentence: "any https origin, self-hosted preferred; JavaScript loaders forbidden" (matches ZG-13)
- `content:` rule stated once in §2 ("decorative glyphs, punctuation and counters are allowed; résumé words never only in `content:`") and referenced by section number from `themes/README.md` (no second wording)
- Designer proofing fixture `skeleton/samples/contract.json`: Jordan Hale's `resume.json` plus the two fields the byte-locked sample omits — `basics.image` = `portrait.svg` (a sibling of `contract.html` inside `skeleton/samples/`, so the relative `src` the renderer writes verbatim resolves both from disk and at `/skeleton/samples/portrait.svg`; a neutral vector with explicit `width="96" height="96"` and a `viewBox`, so `naturalWidth` is non-zero) and `education[0].score: "3.9"` — rendered by the crate to `skeleton/samples/contract.html` and test-locked, so `rz-photo` / `rz-photo-img` / `rz-score` can be proofed on a real Skeleton; `themes/README.md` names it as the file to proof those classes on. ZG-14 serves `skeleton/samples/*.{html,json,svg}` under `/skeleton/samples/` and `/skeleton/example.html`, which is why this PBI depends on it
- §9 gains the compatibility rule: "A change that adds no class and no `data-rz-*` attribute — a new attribute outside `data-rz-*` (for example `dir`), or a different element type carrying an existing class (for example `<span class="rz-date">` for an unparseable date, as `.rz-date--present` already is) — is documented in §5 and does not bump `data-rz-schema`. A new class or `data-rz-*` attribute is a minor bump (1.0 → 1.1). A rename or removal is a major bump and is not done." This one rule replaces the two existing §9 bullets it subsumes ("Additive `rz-*` classes require an HTML contract bump …" and "Renames and removals are breaking. Don't."), so §9 states the rule once; the §9 bullets on theme target, `resume-schema` and bridge dialects are untouched, and `data-rz-schema` stays `1.0` (ZG-2's `<span class="rz-date">` and ZG-3's `dir="auto"` / `<span class="rz-link-value">` ship under it by this rule)
- `docs/planning/resumezen-avril-blessed-backlog.md` gets a top banner "Historical — superseded by ZG-* round-1; the `@font-face` prohibition in RZ-5 AC is obsolete"
## Scope out
- Changing any `rz-*` class or the HTML contract version
- Changing `skeleton/resume.json` / `example.html` (RZ-2 byte-lock) — the fixture is a superset copy, not an edit
- The runnable lint (ZG-20)
- Rewriting `qa/MARKET-QUALITY-BAR.md` (QA-owned)
- A `samples/portrait.svg` path or an absolute URL for `basics.image` (rejected: `samples/…` from inside `skeleton/samples/` resolves to `/skeleton/samples/samples/portrait.svg` → 404; an absolute path breaks the from-disk proof)
## Acceptance criteria
- [ ] `grep -n "dark ink on white" skeleton/CLASS-CONTRACT.md themes/README.md` hits both; `grep -n "unless you mean to" skeleton/CLASS-CONTRACT.md` prints nothing
- [ ] A node test `frontend/scripts/blank-theme.test.mjs` (`node --test`) extracts every backtick-quoted `rz-*` token from `CLASS-CONTRACT.md` §6 (the text between the `## 6.` and `## 7.` headings), expands `rz-contact--{type}` and `rz-link--{type}` with the tables above (5 + 11 literals), drops `data-rz-*` attribute names from the class check, and asserts each resulting class appears as a `.class` selector in `themes/_blank.css`; it also asserts `_blank.css` contains an `@media print` block and a `@media (prefers-reduced-motion: reduce)` block; the test fails if §6 still contains a `{` token that is not in the expansion table
- [ ] The same test asserts `_blank.css`'s header comment contains each of `Name:`, `Author:`, `URL:`, `License:`, `rz-target:` exactly once
- [ ] `_blank.css` is still skipped by `generate.mjs` and the switcher shows no `Blank` card
- [ ] `grep -n 'rz-skill-groups' skeleton/CLASS-CONTRACT.md` hits inside the §5.10 code block
- [ ] `grep -n "any https origin" skeleton/CLASS-CONTRACT.md` hits inside §2; `grep -n "JavaScript loaders forbidden" skeleton/CLASS-CONTRACT.md` hits
- [ ] `grep -n "never only in" skeleton/CLASS-CONTRACT.md` hits exactly once (inside §2); `grep -n -E "never only in|résumé words" themes/README.md` prints nothing, and `grep -n "§2" themes/README.md` hits (the README references the contract section instead of restating the rule)
- [ ] `cargo test --test acceptance` in `renderer/` locks `skeleton/samples/contract.json` → `skeleton/samples/contract.html` `.rz-resume` bytes; `contract.html` contains `class="rz-photo"`, `class="rz-photo-img"`, `src="portrait.svg"`, `alt="Portrait of Jordan Hale"`, and an `rz-score` element whose text is `GPA 3.9`
- [ ] `curl -o /dev/null -w "%{http_code} %{content_type}"` against the served Garden returns `200 image/svg+xml` for `/skeleton/samples/portrait.svg` and `200` for `/skeleton/samples/contract.html`
- [ ] Probe `ZG-19/photo-loads`: opening the served `/skeleton/samples/contract.html` in Playwright, `img.rz-photo-img` has `complete === true` and `naturalWidth > 0`, `fetch(new URL(img.src, location.href))` from the page returns 200, `.rz-score` `textContent` is `GPA 3.9`, and zero console errors / `pageerror`s are recorded (a `samples/samples/` path or a missing SVG fails on `naturalWidth` and on the console error)
- [ ] The same node test asserts every `rz-*` class present in `skeleton/example.html` is also present in `skeleton/samples/contract.html` (superset, never a fork); `git diff --stat skeleton/resume.json skeleton/example.html` is empty for this PBI's diff
- [ ] `grep -n "samples/contract.html" themes/README.md` hits
- [ ] §9 compatibility rule, bounded to §9 (`## 9.` to end of file): `sed -n '/^## 9\./,$p' skeleton/CLASS-CONTRACT.md | grep -c "documented in §5 and does not bump"` prints `1`, `sed -n '/^## 9\./,$p' skeleton/CLASS-CONTRACT.md | grep -c "minor bump (1.0 → 1.1)"` prints `1`, and `sed -n '/^## 9\./,$p' skeleton/CLASS-CONTRACT.md | grep -c "major bump and is not done"` prints `1`; `sed -n '1,/^## 9\./p' skeleton/CLASS-CONTRACT.md | grep -c -E "minor bump|major bump"` prints `0` (the rule lives only in §9); `grep -c "Additive .rz-\*. classes require" skeleton/CLASS-CONTRACT.md` prints `0` and `grep -c "Renames and removals are breaking" skeleton/CLASS-CONTRACT.md` prints `0` (replaced, not kept beside the new rule); `grep -n 'data-rz-schema="1.0"' skeleton/example.html` still hits (no version change)
- [ ] `head -5 docs/planning/resumezen-avril-blessed-backlog.md` contains `Historical`
- [ ] Probe `ZG-19/blank-readable`: applying `/themes/_blank.css` to the served `/skeleton/example.html` in Playwright (swap the `<link>` href): `.rz-name` `isVisible()` is true, its `textContent` is `Jordan Hale`, and zero console errors / `pageerror`s are recorded
- [ ] `just verify` green
## Dependencies
- ZG-14 (serves `/skeleton/example.html` and `skeleton/samples/*` — `blank-readable`, `photo-loads` and the README's proofing link need the served paths)
- ZG-16 (adds `URL:` to the `_blank.css` header)
- ZG-17 (settles the `License:` wording the header carries)
## Notes
- none

## Definition of Done (common)

- Every acceptance-criteria checkbox on the PBI has recorded evidence (test name, probe line, or screenshot path) in `## Execution Evidence`.
- Reviewer, Tester, and Architect each emitted an explicit `BLESS` on every phase.
- `just verify` is green (fmt, clippy pedantic, cargo test, frontend probes).
- Chrome has no `rz-` class or id; Themes target only `html`/`body`/`.rz-*`/`[data-rz-*]`; JSON Resume is the only stored Resume.
- Commit messages reference the PBI id; `progress.md` and `features.json` updated.
