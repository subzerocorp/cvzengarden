+++
id = "ZG-13"
title = "Self-host first-party theme fonts so a résumé page never calls jsDelivr"
status = "in-progress"
rank = "u"
labels = ["avril", "round-1", "themes", "avril-blessed"]
start_at = "2026-08-28T03:00:00.000000Z"
created = "2026-08-23T23:55:59.163590Z"
updated = "2026-08-28T03:00:00.000000Z"
+++

## Why
Every first-party theme leaks each visitor's IP to jsDelivr and loses its face when printing offline; a hosted résumé page must not phone a CDN (Marcus, Mika; BAR-L1).
## Covers
F6 — Marcus, Mika
## Scope in
- Vendor the exact woff2 files the three themes reference (EB Garamond, IBM Plex Sans, Syne, Outfit — all SIL OFL) under `themes/fonts/<family>/` with each family's `OFL.txt`
- `@font-face src` in `themes/*.css` becomes `local("…")`, then `url("fonts/…")` (relative to the CSS file); no `cdn.jsdelivr.net`
- `frontend/scripts/copy-dist.mjs` copies `themes/fonts/` to `dist/themes/fonts/`; `serve.mjs` already maps `.woff2` → `font/woff2`
- `themes/README.md` states the locked fonts policy (`docs/plans/fonts-decision.md`): first-party Themes self-host from `themes/fonts/` (the seed of the Font Library); Designers may submit original fonts to the Font Library under CC BY 4.0; submitted Themes may also `@font-face` / CSS-`@import` any public font from an HTTPS CDN (no host allowlist); JS webfont loaders remain forbidden. This paragraph is the C8 font-allowlist answer.
- Probe `BAR-L1` (owner: this PBI — no such probe exists today): for each pair of first-party themes, `.rz-name` computed `color` or computed `font-family` differs; prints `BAR-L1 PASS|FAIL`
- Probe: request log during a full theme cycle and print has no third-party host
- Console-error convention for route-aborted probes (this PBI's `fallback`): Chromium logs `Failed to load resource: net::ERR_FAILED` as a console message of type `error` for every aborted request, so the probe forbids `pageerror` and every console error **except** those the abort itself causes (identified by `msg.location().url` or `msg.text()` containing `/themes/fonts/`) — the same `pageerror`-only stance ZG-4's `wasm-load-failure` takes
## Scope out
- Subsetting/optimising fonts beyond the latin files already used
- Font Library *submit product* (intake, review, hosting of third-party original fonts) — later AVRIL round; this PBI only vendors the first-party seed and writes the policy
- Changing typefaces
- Re-baselining print page counts — a same-face swap must not reflow; if it does, the PBI returns to planning rather than editing the constants
- A host allowlist of CDNs (locked: none)
## Acceptance criteria
- [ ] `grep -rn "jsdelivr" themes/*.css` prints nothing; `grep -c "@font-face"` reports `quarto.css` 4, `switchyard.css` 4, `nightgarden.css` 5 (today's counts — no face added or dropped)
- [ ] Probe `ZG-13/no-third-party`: cycling all three themes and printing, the Playwright request log contains only `127.0.0.1`/`localhost` URLs
- [ ] Probe `ZG-13/font-files`: every `url(…)` inside every `@font-face` block of `themes/*.css` (parsed with `extractMediaBlocks`-style brace matching), resolved relative to `/themes/`, fetches from the served origin with status `200` and `Content-Type: font/woff2`, and the same path exists under `frontend/dist/themes/fonts/`; the list is non-empty (a theme with no `@font-face` urls fails)
- [ ] Probe `ZG-13/fonts-load`: after selecting Quarto, inside the iframe `await document.fonts.ready` then `[...document.fonts].some(f => f.family.replace(/["']/g,'') === 'EB Garamond' && f.status === 'loaded')` is `true`; same for `IBM Plex Sans` under Switchyard and `Syne` under Nightgarden (loaded from `local()` or from `url()` — either satisfies; `document.fonts.check` is not used); additionally no `FontFace` whose family is one of those three has `status === 'error'` (a broken relative `url()` masked by a working `local()` on the dev machine fails here)
- [ ] Probe `ZG-13/fallback`: in a fresh context with `**/themes/fonts/**` requests aborted via `page.route(…, route => route.abort())`, after cycling the three themes: no `pageerror` is recorded, and no console message of type `error` is recorded other than resource-load failures whose `location().url` (or text) contains `/themes/fonts/` (those are the aborts themselves and are counted, not failed on); `.rz-name` `getBoundingClientRect().height` > 0, the S2 date-geometry probes are green, and `documentElement.scrollWidth <= clientWidth` (BAR-U2); the probe prints the number of ignored font-load errors on its pass line
- [ ] `themes/fonts/*/OFL.txt` exists for every vendored family; `themes/README.md` contains `Font Library`, `CC BY 4.0`, and `HTTPS CDN` (the locked policy, not "any https origin")
- [ ] Probe `BAR-L1`: for each of the three theme pairs, `.rz-name` computed `color` differs or computed `font-family` differs; prints `BAR-L1 PASS`
- [ ] `git diff -- frontend/scripts/probes.mjs` for this PBI does not touch the `U3_PRINT_PAGES` (or `LONG_PRINT_PAGES`, if present) constants, and the S3/U3 page-count probes are green against them
- [ ] `just verify` green
## Dependencies
- none
## Notes
- **Fonts locked 2026-08-24** (`docs/plans/fonts-decision.md`): Font Library inbound CC BY 4.0; OFL seeds keep OFL + `OFL.txt`; submitted Themes may use any public HTTPS CDN font (no host allowlist, CSS only); first-party never phones a CDN.
- Vendoring the OFL woff2 seed (~10 files) is an asset addition outside the crate allowlist — still required for this PBI; human approved the policy. Execution AC for first-party self-host is unchanged, so this amendment is not re-chained through AVRIL.

## Definition of Done (common)

- Every acceptance-criteria checkbox on the PBI has recorded evidence (test name, probe line, or screenshot path) in `## Execution Evidence`.
- Reviewer, Tester, and Architect each emitted an explicit `BLESS` on every phase.
- `just verify` is green (fmt, clippy pedantic, cargo test, frontend probes).
- Chrome has no `rz-` class or id; Themes target only `html`/`body`/`.rz-*`/`[data-rz-*]`; JSON Resume is the only stored Resume.
- Commit messages reference the PBI id; `progress.md` and `features.json` updated.
