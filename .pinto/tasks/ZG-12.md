+++
id = "ZG-12"
title = "Fit long names in Nightgarden, paint sections without scrolling, keep bullet line breaks"
status = "todo"
rank = "t"
labels = ["avril", "round-1", "themes"]
created = "2026-08-23T23:55:59.150691Z"
updated = "2026-08-23T23:55:59.150691Z"
+++

## Why
"Okafor-Lindqvist" paints past Nightgarden's rail, sections below Experience are invisible to crawlers and Cmd-A until scrolled, and a highlight with embedded sub-bullets collapses to one run-on line in every theme (Marcus, Mika).
## Covers
E3 (name overflow, `rz-rise`), D5 (highlight newline) — Marcus, Mika
## Scope in
- `themes/nightgarden.css` `.rz-name`: `overflow-wrap: anywhere; hyphens: auto;` and a container-query or `clamp` step that fits ≥ 24-character names inside `.rz-identity`
- `rz-rise`: start state readable (opacity ≥ 0.999 is not required — the rule is content painted): gate the hidden start state on `@supports (animation-timeline: view())` **and** honour `prefers-reduced-motion`; outside that support block sections start at `opacity: 1`
- All three first-party themes: `.rz-bullet, .rz-prose p { white-space: pre-line }`
- Contract §5.3 note: highlights may contain `\n`; themes should respect it (`white-space: pre-line`)
## Scope out
- Print rules (ZG-11)
- Rendering nested lists from indented text (JSON Resume highlights are strings; no markdown-in-JSON per CONTEXT)
- Any change to Quarto/Switchyard name sizing (no complaint)
## Acceptance criteria
- [ ] Probe `ZG-12/name-fits`: Nightgarden with `basics.name` = `Marcus Okafor-Lindqvist Jr.` at 1280×800 has `.rz-name` `scrollWidth <= clientWidth` and `.rz-identity` `scrollWidth <= clientWidth`; same at 390px
- [ ] Probe `ZG-12/painted-before-scroll`: Nightgarden on `skeleton/example.html` at 1280×800 without scrolling, `getComputedStyle('#rz-volunteer').opacity` is `1` when `animation-timeline` is unsupported (Playwright Firefox/WebKit) and, in Chromium, a full-page screenshot's `#rz-projects` region is not uniformly background-coloured
- [ ] Probe `ZG-12/reduced-motion`: with `prefers-reduced-motion: reduce` emulated, no `.rz-section` has a running animation and all have `opacity: 1`
- [ ] Probe `ZG-12/pre-line`: a highlight `"Store:\n- Postgres\n- moved tables"` renders as three visual lines in each first-party theme (`.rz-bullet` `clientHeight` ≥ 2.5 × its `line-height`)
- [ ] BAR-T1 / BAR-L1 unchanged; existing S3 print probes green
- [ ] `just verify` green
## Dependencies
- none
## Notes
- none
