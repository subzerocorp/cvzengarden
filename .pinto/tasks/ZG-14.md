+++
id = "ZG-14"
title = "Serve and link the designer kit: sample HTML, contract, blank CSS, second sample"
status = "todo"
rank = "v"
labels = ["avril", "round-1", "avril-blessed"]
depends_on = ["ZG-7"]
created = "2026-08-23T23:56:49.161016Z"
updated = "2026-08-24T01:13:07.450718Z"
+++

## Why
Mika and Devon could only find `rz-*` because someone handed them the repo path; from the served Garden the sample HTML, the contract and the blank starting file all 404 (BAR-D1 FAIL) (Mika, Devon).
## Covers
C4, C8 (serve the fuller sample) — Mika, Devon
## Scope in
- `frontend/scripts/copy-dist.mjs` publishes to `dist/`: `/skeleton/example.html` (copied with the `preview.css` `<link>` replaced by an inlined minimal readable `<style>` so BAR-T2 still 404s `/skeleton/preview.css`), `/skeleton/CLASS-CONTRACT.md` (`text/markdown`), `/themes/_blank.css`, `/themes/README.md`, and `skeleton/samples/*.{html,json,svg}` when present (ZG-6)
- `_blank.css` stays out of the switcher catalog (`generate.mjs` SKIP unchanged)
- "For designers" section in the About panel (ZG-7) with links: Sample HTML, Second sample (when present), Class contract, Blank theme file, Theme rules, GitHub repo
- `serve.mjs` sends `text/markdown; charset=utf-8` for `.md`
## Scope out
- A separate `/design` marketing page
- Rendering Markdown to HTML in the chrome (GitHub renders it; the raw file is enough and stays byte-true)
- Submission link / CONTRIBUTING (ZG-18); local CSS try-out (ZG-15)
## Acceptance criteria
- [ ] `curl -o /dev/null -w "%{http_code}"` against the served Garden returns `200` for `/skeleton/example.html`, `/skeleton/CLASS-CONTRACT.md`, `/themes/_blank.css`, `/themes/README.md` and `404` for `/skeleton/preview.css`
- [ ] `curl -s /skeleton/example.html` contains `data-rz-schema="1.0"`, `Jordan Hale`, and no string `preview.css`; opened in Playwright it has no console error and `.rz-name` is visible
- [ ] `curl -sI /skeleton/CLASS-CONTRACT.md` has `Content-Type: text/markdown`
- [ ] Probe `ZG-14/designer-links`: the About panel contains `<a>` elements whose `href`s resolve (fetch → 200) to each of the four assets above and to `https://github.com/subzerocorp/cvzengarden`
- [ ] `frontend/scripts/probes.mjs` still asserts `_blank.css` is not in `Generated/Themes.elm` and not in any `#theme-option-*`
- [ ] Probe `ZG-14/BAR-D1`: prints `BAR-D1 PASS` only when sample HTML 200, blank CSS 200, and a visible submit link exist; until ZG-18 lands it prints `BAR-D1 PENDING ZG-18` (never PASS) — `qa/MARKET-QUALITY-BAR.md` itself is unchanged
- [ ] `just verify` green
## Dependencies
- ZG-7
## Notes
- none
