# AVRIL round 1 — Visionary CTO review (cycle 3)

**Role:** `visionary-cto-agent` (third and final adversary) · **Date:** 2026-08-24 · **Cycle:** 3 (fresh review of my two cycle-2 REJECTs after the Generator's cycle-6 revise, the PO's cycle-6b BLESS and QA's cycle-6b BLESS)
**Scope:** ZG-19 and ZG-21 only. Both are judged from scratch against the cycle-1 target shape; my cycle-2 verdicts lapsed with the body edit. ZG-22 was read because it consumes ZG-21's delete and error-body semantics; it receives no verdict. ZG-11, ZG-12, ZG-20 and the new ZG-23 are outside this cycle.
**Inputs read in full:** `docs/plans/round-1-avril-cto-round2.md` (the two exact sentences I demanded and the cycle-3 checklist I wrote for myself), `round-1-avril-generator-round6.md`, `round-1-avril-po-round6b.md`, `round-1-avril-qa-round6b.md`, the three current bodies (`pinto show ZG-19 ZG-21 ZG-22 --plain` and the tracked `.pinto/tasks/*.md`), `git log` / `git show 11ae2ec` on the three task files, `skeleton/CLASS-CONTRACT.md` §8–§9 as it stands today (lines 556–576; the two old bullets are still there, so every ZG-19 grep is a real change), and `backend/README.md` (still "Not started" — nothing has been built ahead of the plan).

Mandate: *BLESS only backlog items that advance a coherent two-year trajectory without irreversible entanglement; REJECT anything that buys short-term motion at the cost of future clarity.*

I judge trajectory only. Product value is PO's and settled (cycle 6b); falsifiability is QA's and settled (cycle 6b). I do not rewrite bodies and I do not touch the board. The target shape from cycle 1 is unchanged: one Skeleton under a versioned contract with additive-only bumps; one pure Renderer linked twice; Themes as self-describing files; one Store adapter selected by environment; a Designer gate that is a PR today and a Submission later; Author identity as a capability (edit key) that Accounts later claim rather than replace.

## Facts I verified before judging

| Fact | How | Affects |
| --- | --- | --- |
| The only board edit since my cycle-2 review is commit `11ae2ec` ("generator revise 6, PO cycle 6"): ZG-19 +2 lines (one `Scope in` bullet, one AC), ZG-21 seven altered lines, none added or removed; ZG-22 untouched. Both `updated` timestamps (`2026-08-24T01:30:20Z`) match that commit and nothing later. | `git log -- .pinto/tasks/ZG-19.md ZG-21.md ZG-22.md`, `git show --stat 11ae2ec` | "nothing else moved" is a fact, not a claim; my cycle-2 reading of every untouched sentence carries |
| ZG-19 `Scope in` contains my §9 sentence verbatim — every clause, both worked examples (`dir`, `<span class="rz-date">`), the `.rz-date--present` precedent, "minor bump (1.0 → 1.1)", "major bump and is not done". | string comparison against `round2.md` | the blocker |
| ZG-19's §9 AC is bounded by `sed -n '/^## 9\./,$p'`, counts three positive tokens to 1, two removal tokens to 0 (`Additive .rz-\*. classes require`, `Renames and removals are breaking` — both 1 today), a "only in §9" negative to 0, and pins `data-rz-schema="1.0"` in `example.html`. | AC read; today's contract lines 572–573 | the bounded grep I asked for, with the old line replaced not kept |
| ZG-21 `Scope in` contains my tombstone sentence verbatim; `grep -c 'removes the row'` on the body prints 0; the Store bullet's table is `resumes(…, deleted_at NULL)` with "NULL until DELETE; `edit_key_hash` and `resume_json` are NULL only on a tombstone"; `delete_then_gone` reads the row by SQL after the 204 and asserts row present, `resume_json IS NULL`, `edit_key_hash IS NULL`, `deleted_at IS NOT NULL`. | body read, grep | the blocker, the column, the assertion |
| ZG-21 still says: `thiserror` errors mapped in one place; pure `domain` / `store` / `http` layering; one libSQL store, `TURSO_DATABASE_URL` + `TURSO_AUTH_TOKEN` switch "with no code change"; "No CORS layer"; "Storing rendered HTML" in `Scope out`; `/r/{id}` with 10-char base32 from 50 bits; edit key returned once, stored as SHA-256, `Authorization` only; wrong/absent key 404; no `/api/render`; theme validated against "a catalog id". | grep on the body | no strategic regression |
| ZG-22's `delete` probe (410 + `taken down`), trust copy ("Kept until you delete it") and privacy panel are unchanged and satisfied by the tombstone. | `pinto show ZG-22`, `git show --stat` | the consumer needs no edit |

---

## ZG-19 — Put the print-honesty rule, a full blank scaffold, and contract fixes where designers look

**The blocker is closed exactly as asked.** In cycle 2 I said the round's designated contract editor left §9 saying "Additive `rz-*` classes require an HTML contract bump … Renames and removals are breaking", contradicting the no-bump changes ZG-2 (`<span class="rz-date">`) and ZG-3 (`dir="auto"`, `<span class="rz-link-value">`) already ship under 1.0, and that the meaning of `data-rz-schema` — the only version a Designer ever sees — must be a contract rule and not a planning-doc opinion. The current `Scope in` carries my sentence verbatim as a §9 edit, and the AC is one bounded grep (`## 9.` to end of file) that fails on a stub that skips the edit, appends without replacing, bumps the version to "fix" it, or renames the heading. The rule now states the three cases mechanically: a change that adds no class and no `data-rz-*` attribute is documented in §5 and does not bump; a new class or `data-rz-*` attribute is 1.0 → 1.1; a rename or removal is major and is not done. Execution rule 15 ("Contract §9 is the rule, the CTO doc is not") becomes literally true the day this lands.

**The one deviation from my wording is the right one.** I named only the "Additive" line for replacement; the Generator also replaces "Renames and removals are breaking. Don't." because the new sentence's last clause restates it, and keeping both would leave §9 with two wordings of one rule — the condition my "one rule, not two" forbids. Correct. The three §9 bullets that are not about the HTML contract's own version (theme target does not bump, `resume-schema` tracked upstream, bridge dialects live in `converter/`) stay, and the AC's positive token is lengthened to `documented in §5 and does not bump` precisely because the theme-target bullet already says "It does not bump `data-rz-schema`" — the Generator caught that a bare `does not bump` would count 2 on a correct edit. That care is what I want from a contract editor.

**Coherence with the rest of the contract.** §5 line 489 today reads "No other `rz-*` classes exist in HTML contract `1.0`. If you need a new one, bump `data-rz-schema` — do not invent it in a single theme." That is the "new class is a minor bump" case in §6's own words, so after the edit §5 and §9 agree. The "rule lives only in §9" negative (`minor bump|major bump` absent before the heading) is the same one-wording discipline the item already applies to the `content:` rule, and it is the reason a fifth contradicting document cannot appear later without failing this AC.

**Nothing else moved.** Two lines added, none altered (verified by the revise diff). The paper rule in §2 and the README, the §6-projected `_blank.css` with its node test, the §5.10 wrapper, the one fonts sentence, the one `content:` rule, the `contract.json` / `contract.html` proofing fixture as a superset copy with the RZ-2 lock untouched, the historical banner — all as I blessed the shape of in cycle 2. `Scope out` still refuses any class change and any contract-version change; the AC pins `data-rz-schema="1.0"` in `example.html`. Dependencies (ZG-14, ZG-16, ZG-17) and the dependent (ZG-20, which lints CSS and does not read §9) are unchanged. No new edge is needed; the `CLASS-CONTRACT.md` overlap with ZG-2 (§5.3) and ZG-3 (§3) is different sections.

**Strategic consequence.** With this sentence in the contract, the code GAN's architect judges "bump or no bump" by §9, ZG-2 and ZG-3 ship under 1.0 by the contract's own rule, and the first deliberate 1.1 (`work[].description`, named in ZG-3's `scope_out`) is a planned item with a stated version, not a bug fix. Designers get one version that moves only when the selector surface moves. Reversible (a doc edit), no entanglement.

Guidance for AXEL (unchanged, not blocking): the `contract.html` lock is a row in the one table-driven golden-file helper (execution rule 3), regenerated by ZG-3's `dir="auto"` if ZG-3 lands later; `blank-readable` injects a `<link>` into ZG-14's served `example.html`; QA's optional `for example \`dir\`` token may be folded without re-review.

**BLESS ZG-19 — the §9 compatibility sentence is present verbatim with a bounded grep that replaces both old bullets rather than sitting beside them, so `data-rz-schema` means one thing in the contract, in §5, and in the two already-blessed no-bump items; nothing else in the body moved (two lines added, verified by diff), no version or class changes, no new edge, fully reversible.**

## ZG-21 — Publish a résumé to a stable public URL with an unguessable edit link (API)

**The blocker is closed exactly as asked.** In cycle 2 the body said both "`DELETE` removes the row" and "after DELETE, `GET /r/{id}` is 410 forever" on a schema with no column for a delete state, leaving the execution team to invent the one irreversible data-lifecycle rule — and the cheapest invention (a soft-delete flag with `resume_json` kept) would have made ZG-22's "Kept until you delete it" false for exactly the users who pressed Delete, uncorrectably. The current `Scope in` has "removes the row" gone and my tombstone-and-erase sentence verbatim in its place: `resume_json` and `edit_key_hash` set to NULL, `deleted_at` set to now, the `id` row kept so `/r/{id}` answers 410 forever and an id is never reissued, and a direct SQL read after DELETE finds no résumé bytes and no key hash. The schema gains `deleted_at NULL` with the invariant stated ("NULL until DELETE; `edit_key_hash` and `resume_json` are NULL only on a tombstone"). `delete_then_gone` reads the row by SQL after the 204 — the one assertion that fails a hard delete (row absent) and a soft-delete-with-bytes (row non-NULL) alike. One sentence, one column, one assertion, as I priced it.

**The data-lifecycle rule is now the seam Phase 5 builds on, not a decision it must reopen.** Retention automation later is a query over `deleted_at` / `updated_at` (execution guidance 13); Accounts later claim a live row by presenting the key once and the table gains `account_id`; neither touches the delete semantic or the `/r/` space. The status matrix after DELETE is consistent everywhere it appears (`delete_then_gone`, `put_and_delete_require_key`, `public_page_headers_and_purity`): API paths 404 (the hash to compare against is NULL, so no key can ever match — existence never revealed), `/r/{id}` 410 with the same `no-store` / `noindex` headers. ZG-22 needs no edit; its trust copy becomes structurally true rather than asserted, and decision 5's retention wording can be answered as proposed.

**The three QA folds are additive and do not bend the shape.** A seventh error code `method-not-allowed` with the router's fallbacks (unmatched `/api/` path → 404, wrong method on a matched route → 405) in the one `{error, message}` JSON shape is the "one `IntoResponse` produces every body" rule of guidance 10 applied to Axum's own fallbacks — it closes the last hole in "every non-2xx under `/api/`" instead of adding an exemption, which is the right call for a client (ZG-22) that shows `message` and falls back to `error`. `theme` absent → 422 `unknown-theme` names the field, no new code. The 1 MiB limit counted on bytes received rather than `Content-Length` is what `DefaultBodyLimit` already does; the AC only stops a naive header check from passing. No new endpoint, no new field, nothing an Author reaches through the chrome.

**No strategic regression.** Re-read against my cycle-2 list: layering stays pure `domain` / `store` (libSQL actions) / `http`; errors are `thiserror` mapped to status + body in one place; one store implementation selected by environment with the Turso switch "no code change" (lock 7 without a second adapter; the counting "test store wrapper" is the only other `Store` impl); `/r/{id}` with 10-char base32 from 50 random bits and `/api/`, `/themes/`, `/skeleton/` as separate prefixes, so slugs later take `/{slug}` or `/u/{slug}` without touching `/r/`; edit key as a bearer capability returned once, stored as SHA-256, `Authorization` only, 404 on wrong or absent; render on read from the verbatim JSON of record with stored HTML still in `Scope out`; no CORS layer, so the deploy topology stays a human decision; no `/api/render`; `clippy::pedantic` and `grep -rn anyhow` empty as gates. The theme check is "a catalog id", which leaves the catalog's source to guidance 14 (the `dist/themes/*.css` listing minus `_blank.css`, never a Rust array) — that remains a code-review rule, not an AC, and that is the correct altitude for it. Seven altered lines, none added or removed; dependencies (ZG-2, ZG-3) and the dependent (ZG-22) unchanged; `backend/README.md` is still "Not started", so nothing was built ahead of the ruling.

Guidance for AXEL (not blocking; cycle-2 guidance 10–14 stands): the "by SQL" reads in `delete_then_gone` and `edit_key_not_stored_plain` go through the store's own connection when `:memory:` is in use (QA's note — a wrong connection yields a false FAIL, never a false PASS); the store's insert must refuse an existing id (live or tombstone) and the handler retries the random draw — QA's suggested store-level unit case is the cheapest way to make "never reissued" falsifiable and I want it, but the observable promise is already covered by the tombstone read; `OPTIONS /api/resumes` becoming a JSON 405 is correct and `no_cors` asserts only header absence; `render_document(resume, theme_id) -> String` remains the single seam where the theme `<link>` is inserted after `<title>`; expose `CONTRACT_VERSION` on a header or `/api/version` (execution rule 8, additive). Human decisions 4 (`sha2` / `rand`), 7 (deploy path) and 8 (no public `POST` on Turso without a rate limit) are unchanged and must be surfaced before AXEL starts this item.

**BLESS ZG-21 — the tombstone-and-erase sentence is present verbatim with `deleted_at` in the schema and the SQL read in `delete_then_gone`, so the one irreversible data-lifecycle rule is stated in the body, makes ZG-22's trust copy structurally true, and is the seam retention and accounts will query rather than reopen; the three folds close holes in a promise the body already made without a new endpoint, field or code path; layering, single env-selected store, capability edit key, stable `/r/{id}` space, render-on-read and no-CORS all stand unchanged (seven altered lines, verified by diff).**

---

## Summary

| id | PO c6b | QA c6b | CTO c3 | one-line reason |
| --- | --- | --- | --- | --- |
| ZG-19 | BLESS | BLESS | **BLESS** | §9 sentence verbatim, bounded grep, both old bullets replaced not kept; `data-rz-schema` has one meaning across §5, §9, ZG-2 and ZG-3; two lines added, nothing else moved |
| ZG-21 | BLESS | BLESS | **BLESS** | tombstone-and-erase verbatim, `deleted_at` in schema, SQL read in `delete_then_gone`; three additive QA folds; layering / single store / capability key / `/r/{id}` / render-on-read / no-CORS unchanged |

2 BLESS, 0 REJECT. ZG-19 and ZG-21 now carry PO, QA and CTO BLESS and may be labelled `avril-blessed`. With ZG-13 and ZG-17 from cycle 2, the Designer chain `ZG-16 → ZG-17 → ZG-19 → ZG-20` and the Author chain `ZG-1 → ZG-2 → ZG-3 → ZG-21 → ZG-22` each now have one open item left (ZG-20 and, in a separate lane, ZG-11 / ZG-12 with the Generator), plus the new harness item ZG-23, which I have not reviewed.

## Set-level notes

- **Graph unchanged.** No new edge is asked for by either item. File-overlap ordering stands: `themes/README.md` — ZG-13 first, then ZG-17 → ZG-19; `skeleton/CLASS-CONTRACT.md` — ZG-2 (§5.3), ZG-3 (§3), ZG-19 (§2, §5.10, §9) are different sections.
- **Human decisions batch:** unchanged from cycle 2 (1–8; 9 was retired into ZG-19's delta and is now closed). Decision 5's retention wording is now backed by erase-on-delete and can be confirmed as proposed; if the human wants PO's stronger clause ("after delete only the dead link is kept so it can never show anyone else's page"), it is one line in ZG-22's panel, not a ZG-21 change.
- **Execution guidance for AXEL:** cycle-1 rules 1–9 and cycle-2 rules 10–16 stand verbatim. Rule 13 ("Delete is tombstone-and-erase") and rule 15 ("Contract §9 is the rule") are no longer conditional on a revise landing — the bodies now say it.
- **What I will look for when ZG-11 / ZG-12 / ZG-20 return (and ZG-23 if it is routed to me):** probe helpers in `probes/lib/`, `U3_PRINT_PAGES` / `LONG_PRINT_PAGES` re-baselined exactly once (ZG-11), `checkTheme` pure over a shared `theme-header.mjs`; for ZG-23, that the paint-order oracle replaces the sampler rather than sitting beside it and that no retry or `SKIP` path is introduced.
