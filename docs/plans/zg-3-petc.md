# ZG-3 PETC — Render wild JSON Resume files: numeric score, url-only entries, CRLF, bad URLs

**PBI:** ZG-3 (triple-blessed) · deps ZG-1, ZG-2 done
**AXEL:** Generator `code-writer` + `rust-code-writer` + `rust-errors`; adversaries `rust-code-reviewer` → `rust-code-tester` → `rust-architect`
**Decomposition mode:** off

## Goal
Wild files render: numeric score, url-only certificate/publication, profile without url, CRLF prose, unsafe/empty URLs never as href, emoji-only slugs get `entry`/`skill` fallbacks, `dir="auto"` on `.rz-resume`; no empty `<li>` anywhere; locks regenerated in the same commit.

## Approach
Serde helper for `score`; a pure `url.rs` calc (`safe_href(&str) -> Option<&str>`, `hostname`) used by profiles/contacts/entry links; prose normalisation; slug fallback in `slug.rs`; `dir="auto"` in `emit_article`. Regenerate `skeleton/example.html` (hand-maintained reference; update the article tag only) and `frontend/fixtures/long-resume.html` via `RZ_UPDATE_FIXTURES=1`; contract §3 (`dir`), §5.2 (span value, omission rules, "omitted entirely"), §5.4 note. Tests appended to `wild.rs`.

## Files likely
`renderer/src/{resume,emit,slug,url}.rs`, `renderer/tests/wild.rs`, `skeleton/{example.html,CLASS-CONTRACT.md}`, `frontend/fixtures/long-resume.html`, `frontend/generated/sandbox.html` (regenerated by build).

## Test strategy
One named test per AC; the two "matches nothing" regexes run over every rendered fixture in `renderer/tests/`; BAR-R1 Ada test still green; acceptance + fixtures locks green after regeneration; `just verify` (frontend probes: sandbox regenerated from example.html — S-probes and ZG-11/12 must stay green).

## Risk
`dir="auto"` touches every lock and the sandbox; frontend probes re-run.

## Phases
1. **Phase 1 of 1**

## Unresolved questions
- none (CTO ruled in cto-round1: `dir` and `<span class="rz-link-value">` are non-breaking under 1.0)
