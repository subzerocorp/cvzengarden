# ZG-11 GAN report

## Phase 1 — Reviewer

Mandate: make every piece of code so clear, layered, and correct that any experienced developer can understand and safely modify it in under 10 minutes.

Scope reviewed: `renderer/tests/fixtures.rs`, `frontend/fixtures/long-resume.{json,html}`, `frontend/scripts/probes/lib/{page,print-geometry,contrast,print-ink,pdf}.mjs`, `frontend/scripts/probes/zg-11.mjs`, diff of `frontend/scripts/probes.mjs`. No product file was edited by this review.

Tooling, re-run by the reviewer (not taken from the evidence file):
- `cargo fmt --all -- --check` clean; `cargo clippy --all-targets -- -D warnings -D clippy::pedantic` clean; `cargo test` 10 + 9 + 2 + 0 passed. `grep allow( renderer/tests/fixtures.rs` empty.
- `PROBE_PORT=4345 npm test` exit 0; ZG-11 group prints 4 PASS + 12 FAIL under the loud banner, closing banner `12 expected FAIL line(s) recorded`; final line `All RZ-3, RZ-S1…S5, U3 print, and ZG-23 probes passed.` Log: `/tmp/persona-lab/conductor/zg11-gan-npm-test.log`.
- `grep -rc SKIP scripts | grep -v ':0'` → only `scripts/generate.mjs:2`, which is the pre-existing `const SKIP = new Set(["_blank.css"])` filename filter (present at HEAD, untouched). No probe prints `SKIP`; no probe is skipped.

Fixture realism vs the PBI counts (`long-resume.json`): name Marcus Okafor-Lindqvist; summary 2 paragraphs; work 4 with 6/6/5/5 highlights of 20–28 words (the PBI's 12–25 range is "a guide, not a ceiling: lean long" and `fixture-trips-e1` holds at 1314.9 px > 952 and 1215.5 px > 1009, so the overage is the PBI's own instruction); volunteer 2; education 2; awards 2; skills 6; projects 3 × 3. HTML: 4 `rz-entry--experience`, 3 `rz-entry--project`, 2 `rz-entry--education`, 35 bullets in `#rz-experience`. Matches.

Rust lock helper (`fixtures.rs`): table-driven (`FIXTURES: &[FixtureRow]`, `FixtureRow { json, html }`), one action `locked_html(row)` (read → render → optional write → assert), one calculation `section_html`, one `count`. `RZ_UPDATE_FIXTURES` read once through `update_requested()` and the empty string is correctly treated as unset (verified: `RZ_UPDATE_FIXTURES= cargo test` does not rewrite). No `allow`, pedantic clean, every fallible call carries the file path in its panic message and the regenerate command in the byte-equality and missing-file messages. Doc comments explain why (drift lock) not what. Nesting depth 1. Accepted.

JS modules:
- `contrast.mjs` is pure end to end; `parseRgb` handles `rgb()/rgba()`, space-separated with `/` alpha, `%` alpha, hex 3/6, `transparent`; alpha is composited over the background before luminance (`rgba(0,0,0,0)` → 1:1, `rgb(0 0 0 / 50%)` → 3.98:1, verified by hand). Unparseable → `null`, which every consumer turns into a FAIL line, never a silent pass.
- `print-geometry.mjs`: data (`PAGE_RULES` table keyed by theme, units declared per row), calculations (`toPx`, `printableWidthPx/HeightPx`, `paginateBlocks`, `page1Fill`, `tallBlocks`, `pageIndexOfY`, `splitEntries`), actions (`readAtomicBlocks`, `readEntryHeaderMarks`), and `paginate` as the one composition; the header comment says exactly this. `paginateBlocks` is 20 lines, flat, and I verified by hand: oversize block is placed alone and the following block starts a fresh page; `forcedBefore` closes the page; the exactly-fits boundary (`<=`) is inclusive; `page1Fill` = (last bottom − first top) ÷ H and returns 0 for no pages.
- `print-ink.mjs`: two actions returning plain records, then pure `isGlyph` / `hasBox` / `markerExistsReasons` / `markerInkedReasons` / `bulletMarkerReasons` / `inkReasons` / `describe*`. The three PBI marker clauses (glyph, sized empty-content box, list-style) map one-to-one onto `isGlyph || hasBox || listStyleType !== "none"`. `BOXLESS_CONTENT` = `""`/`none`/`normal` matches the PBI's CSSOM reading.
- `pdf.mjs`, `page.mjs`: moved verbatim from the runner plus `openResumePage` / `useSheetText` / `extractArticle`; `extractArticle` is the only calculation and throws on a fixture without an article. `openResumePage` uses the suite's `chromium.launch({ headless: true })` browser and passes no scrollbar flags, as the PBI requires.
- `zg-11.mjs`: per-PBI orchestration; every probe is `read → reasons() → one pass/fail line`. `widthGuard` implements the "stop with FAIL article width" precondition for the three geometry probes. `forcedBreakReasons` strips comments first so a commented-out `break-before: page` does not trip the static scan (verified). `liveSheets` / `gitSheets` are a two-row strategy table.
- Runner: only imports, the `LONG_PRINT_PAGES = {}` placeholder, the expected-fail reporter, and `zg11Group` (20 lines). `zg11Reporter()` is handed only to `zg11Probes`; `fail` for every other group is untouched, so the mechanism cannot mask anything outside ZG-11. The constant is named for its removal condition, sits beside a `TODO(ZG-11 phase 2): delete`, and the closing banner says "phase 2 makes them count". Obviously temporary.

Nits, not blocking (record for phase 2 or the next touch of these files):
1. `zg-11.mjs` `articleWidthProbe` is declared `async` but contains no `await` — dead async.
2. `zg-11.mjs` `countPages` opens at a magic `width: 1280`; name it (`SCREEN_WIDTH_PX`) next to the other constants.
3. `zg-11.mjs` hard-codes `theme === "switchyard"` twice (`sectionBreakReasons`, `noForcedBreakProbe`). The PBI singles Switchyard out, but a `FORCED_BREAK_SCAN = { switchyard: "rz-projects" }` table would read the same and remove the string comparison from the probe body.
4. `print-geometry.mjs` `readAtomicBlocks` silently drops elements that are neither atomic nor in `LEAF_BLOCK_SELECTOR` and have no qualifying descendants: in the fixture that is every `li` of `ul.rz-meta-list` (`#rz-languages`) and `ul.rz-tags` (`#rz-interests`). Those sections contribute only their `h2` to the simulated pages. Harmless for page-1 fill today (they sit at the end of the document) but a future fixture that puts them early would under-count; either extend the leaf selector (`.rz-meta-list`, `.rz-tags`) or state the omission in the module header.
5. `fixtures.rs`: `long_resume_html_is_crate_output` re-runs the byte lock that `every_fixture_row_is_crate_output` already ran for the same row; under `RZ_UPDATE_FIXTURES=1` the file is written twice. Harmless, but the count assertions could take the rendered string from a plain `render_json` and leave the lock to the table test.
6. The expected-fail reporter also downgrades `article-width` and `fixture-trips-e1`, which the PBI wants green in phase 1; a phase-1 regression there would exit 0. Acceptable only because phase 2 deletes the mechanism; phase 2 must confirm those four lines are PASS before removing the flag.

BLESS ZG-11 phase-1 — fixture matches the PBI counts and provably trips E1 at paper width, the cargo lock is table-driven and pedantic clean with no `allow`, the JS lib is data / calculations / actions with the calcs pure and the runner orchestration-only, and the expected-fail gate is scoped to the ZG-11 reporter, loudly labelled, and prints no SKIP.

## Phase 1 — Tester

Mandate: every calculation and every public item must have clear, fast, exhaustive tests that would catch a regression immediately.

Drift lock, exercised for real (not reasoned): I replaced one byte of `frontend/fixtures/long-resume.html` (`Okafor` → `Okafer`) and ran `cargo test --test fixtures`: both `long_resume_html_is_crate_output` and `every_fixture_row_is_crate_output` FAILED at `tests/fixtures.rs:55` with `frontend/fixtures/long-resume.html is not the crate output for frontend/fixtures/long-resume.json (run RZ_UPDATE_FIXTURES=1 cargo test --test fixtures)`. File restored byte-identical (`cmp` clean). `RZ_UPDATE_FIXTURES=` (empty) does not rewrite. The lock does its job.

Algorithm vs the PBI, checked by driving the pure calcs directly with `node --input-type=module`:
- `paginateBlocks([0–10, 10–150, 150–160], 100)` → `[[b0],[b10],[b150]]`: oversize block alone on its page, the next block begins the following page. Matches the PBI.
- forced break: `[[b0],[b10,b20]]`. Matches `break-before: page` honoured.
- `page1Fill` of `[20–50, 60–110, 115–130]` at H=100 → 0.9 = (110 − 20)/100: the top offset does not leak, the formula is the PBI's.
- `contrastRatio("#000","#fff")` = 21.00; white on white = 1; `rgba(0,0,0,0)` = 1 (composited, not read as black); `oklch(...)` → `null` → reported as `n/a` and failed, not passed.
- `bulletMarkerReasons`: glyph `"– "` passes; `content none / width auto` fails "no marker" (Switchyard's E2 reading); sized bar with `exact` passes; bar with `height 0px` fails; `list-style-type: disc` passes the exists clause.
- `forcedBreakReasons`: comment-only occurrence → no reason; `page-break-before : always` inside `@media print` → reason (the whole-file scan, not `extractMediaBlocks`).
- `printableWidthPx/HeightPx` → 682/952, 673/1009, 720/995. Matches the PBI numbers.

Anti-vacuity readings vs the PBI's expected failure modes: page1-fill 0.36 / 0.30 < 0.85 (E1); no-forced-break Quarto reads `avoid` on every section (the `page-break-inside: avoid` alias winning over `avoid-page`, exactly as the PBI predicts) and Switchyard adds `#rz-projects break-before page` plus the static hit; bullets-print Switchyard fails marker-exists (`content none, width auto`) and Nightgarden fails the ink clause (mint 1.30:1, `economy`), both modes seen once as the AC demands; print-ink Nightgarden `.rz-entry-secondary` 1.84:1 (E3). All match.

Unit tests for the pure calculations: none. `frontend/` has no `node:test` files, `npm test` = build + probes, and no `*.test.mjs` exists anywhere under `scripts/`. This phase adds five `probes/lib/` modules and one per-PBI module carrying `paginateBlocks`, `page1Fill`, `tallBlocks`, `pageIndexOfY`, `splitEntries`, `parseRgb`, `relativeLuminance`, `contrastRatio`, `isGlyph`, `hasBox`, `bulletMarkerReasons`, `inkReasons`, `extractArticle`, `countPdfPages`, `forcedBreakReasons` — all plain-data-in, plain-data-out, no browser needed. The ZG-23 Tester blessed that PBI on the explicit condition (docs/plans/zg-23-gan-report.md line 39): "add `node --test` files under `frontend/scripts/probes/lib/` for the four pure calcs, wired into `npm test`. It must land before the next `probes/lib/` module is added, so the lib grows with tests from its second module on." That condition was not met: five modules landed and the runner is still the only test. The calculations I exercised above behave correctly today, which is exactly why they must be pinned now: phase 2 will judge the theme CSS by `paginateBlocks` / `page1Fill` / `bulletMarkerReasons`, and any later edit to those (the Reviewer's nit 4 on `LEAF_BLOCK_SELECTOR` is one such edit) has nothing to catch a regression except the theme sheets themselves — a circular oracle. "It works on my machine is not an answer"; a hand-driven `node -e` session in a GAN report is not a test.

TEST VERDICT: REJECTED

Decision, explicit: phase 1 must add `node:test` cases. No new dependency (`node:test` and `node:assert` are the standard library), no product file changes, no refactor. Minimal delegation to the Generator (`code-writer`):

Missing tests:
- `frontend/scripts/probes/lib/print-geometry.test.mjs`: `paginateBlocks` — oversize block alone and the next block on a fresh page; `forcedBefore` closes the page; exactly-fits boundary; empty input → `[]`. `page1Fill` — top-offset formula (the 0.9 case above); no pages → 0. `tallBlocks`, `pageIndexOfY` (hit / gap → −1), `splitEntries` (same page / different pages / `datesY === null` ignored). `printableWidthPx/HeightPx` for the three themes = 682/952, 673/1009, 720/995 and unknown theme throws.
- `frontend/scripts/probes/lib/contrast.test.mjs`: `parseRgb` for `rgb()`, `rgba()`, space/slash form, `%` alpha, hex 3 and 6, `transparent`, garbage → `null`, empty → `null`; `contrastRatio` 21 / 1 / composited alpha / `null` on unparseable; `formatRatio(null)` = `n/a`.
- `frontend/scripts/probes/lib/print-ink.test.mjs`: `bulletMarkerReasons` for all five records used above (glyph pass, glyph pale ink, `none`/`auto` no-marker, bar exact pass, bar with `economy` and pale background → both reasons, zero-height bar, `list-style-type: disc`, `null` marker); `inkReasons` for pass / pale / absent.
- `frontend/scripts/probes/lib/page.test.mjs`: `extractArticle` happy path and the throw on missing article. `frontend/scripts/probes/lib/pdf.test.mjs`: `countPdfPages` on a buffer with `/Type /Page` ×2 and `/Type /Pages` ×1 → 2.
- `frontend/scripts/probes/zg-11.test.mjs`: `forcedBreakReasons` — comment-only → `[]`; `@media print { … page-break-before: always }` → one reason; `break-before: page` at top level → one reason; `break-before: auto` → `[]`.
- ZG-23's four pure calcs (`paintStart`, `paintOrderReasons`, `paintPresenceReasons`, `sheetBlockingReasons`) are the follow-up that was owed before this phase; add them in the same `lib/*.test.mjs` layout or record them again as a named debt in this report — they are not what blocks ZG-11.
- `package.json`: `"test"` runs `node --test scripts/probes/**/*.test.mjs` (or `node --test scripts/probes/`) before `npm run probe`, so `just verify` runs them; the unit files must not import Playwright or launch anything.

REJECT ZG-11 phase-1 — the fixture lock fails on a one-byte drift and the paginator, contrast, marker, ink, and forced-break calculations match the PBI's algorithm and reproduce every expected failure mode, but none of the fifteen pure calculations the phase introduces has a unit test, the frontend still has no unit-test runner, and the ZG-23 Tester's blessing was conditioned on that runner landing before the next `probes/lib/` module; phase 1 must add the `node:test` files listed above and wire them into `npm test` before the Tester blesses.

## Phase 1 — Reviewer (pass 2)

Fresh chain after the Generator's fix. Scope: everything in pass 1 plus `frontend/scripts/probes/lib/{contrast,page,paint,pdf,print-geometry,print-ink,sheet-blocking}.test.mjs`, `frontend/scripts/probes/zg-11.test.mjs`, the `package.json` diff, and the three nits folded into `zg-11.mjs`. Re-run by the reviewer: `npm run test:unit` 79/79; `PROBE_PORT=4347 npm test` exit 0, 69 PASS lines, 12 expected FAIL under the banner, no `SKIP`; `cargo fmt --check` / clippy pedantic / `cargo test` (10 + 9 + 2 + 0) clean. `git status` shows only `package.json` and `probes.mjs` modified; no theme sheet, `ports.js`, `generate.mjs`, or `U3_PRINT_PAGES` touched.

Test files: every case is Arrange / Act / Assert in three to five lines, asserts one behaviour, and is named `<function> <behaviour>` so the TAP line reads as the spec (`paginateBlocks moves a block that overflows by one pixel to the next page`). Fixture builders are one local helper per file (`block`, `marker`, `timing`, `doc`) with override spreading; nothing is copied between files and no shared fixture module was invented for eight files that need none. Imports are `node:test` / `node:assert/strict` and the module under test only; `grep -l playwright` over the test files is empty. Assertions are `deepEqual` on the returned reason arrays, so a regression changes the diff, not just a count. The pale-bar case checks `reasons[0]` with a `\d\.\d\d` regex where an exact `1.xx:1` would pin the arithmetic — minor, the composite/luminance arithmetic is pinned exactly in `contrast.test.mjs` (21 / 1 / 3.98).

Folded nits, verified in `zg-11.mjs`: (1) `articleWidthProbe` is now synchronous and is called without `await`; (2) `SCREEN_WIDTH_PX = 1280` sits with the other constants and carries a why-comment; (3) `FORCED_BREAK_SCAN = { switchyard: { sheet, sectionId } }` replaces both string compares — `sectionBreakReasons` and `noForcedBreakProbe` both read the table, no `"switchyard"` literal remains outside it. Runner-side, the local `parseRgb` was deleted in favour of the lib one (a superset: the old regex accepted only the comma form) and the moved ZG-23 actions (`holdThemeSheets`, `releaseThemeSheets`, `waitForSandboxComplete`, `sandboxFrame`) import from `lib/page.mjs`; `coldLoadPaintTiming` now calls `releaseThemeSheets` instead of a raw `unroute` on a duplicated glob. Pass-1 nits 4 (`LEAF_BLOCK_SELECTOR` omits `.rz-meta-list` / `.rz-tags` items), 5 (`long_resume_html_is_crate_output` re-locks the row) and 6 (reporter downgrades `article-width` / `fixture-trips-e1`) are unchanged and remain recorded for phase 2 / ZG-19. New nit, not blocking: `sectionBreakReasons(sections, theme)` looks `FORCED_BREAK_SCAN[theme]` up a second time when `noForcedBreakProbe` already holds `scan`; pass `scan` in and drop the `theme` parameter.

BLESS ZG-11 phase-1 — the eight `node:test` files pin every pure calculation the phase introduces (and the ZG-23 debt) in AAA form with one behaviour per case and no duplicated fixtures, the three folded nits are exactly as asked with no behaviour change (probe output byte-identical to pass 1), the runner is still orchestration-only, and every gate re-run by the reviewer is green.

## Phase 1 — Tester (pass 2)

Every blocker from pass 1, checked against the files:
- `print-geometry.test.mjs` (17): oversize-alone / next-on-fresh-page, `forcedBefore` closes the page, exact fit stays (`<=`), overflow-by-one moves, `[]` → `[]`; `page1Fill` 0.9 top-offset case, no pages → 0, empty first page → 0; `tallBlocks`; `pageIndexOfY` hit and gap → −1; `splitEntries` same page / different pages / `datesY === null`; the three `printableWidthPx/HeightPx` pairs 682/952, 673/1009, 720/995 in one table assert; unknown theme throws for both. All present.
- `contrast.test.mjs` (18): `parseRgb` comma `rgb()`, `rgba()`, space/slash, `%` alpha, hex 3, hex 6, `transparent`, `oklch` → `null`, `""` → `null`; `contrastRatio` 21, 1, transparent composited → 1, 50 % black → 3.98, `null` on unparseable fg and on unparseable bg; `formatRatio(null)` = `n/a` and `4.50:1`. All present.
- `print-ink.test.mjs` (16): glyph pass, pale glyph (1.67:1 exact), `none`/`auto` no-marker with the verbatim reason, sized dark bar with `exact` pass, pale bar on `economy` → both reasons in order, zero-height bar → no-marker, `list-style-type: disc` pass, `null` marker; `inkReasons` pass / pale / absent / unparseable → `n/a` fails. All present.
- `page.test.mjs` (2) happy path with attributes preserved and the throw; `pdf.test.mjs` (2) `/Type /Page` ×2 (one without the space) with a `/Type /Pages` decoy → 2, and 0. `zg-11.test.mjs` (4): comment-only → `[]`, `page-break-before : always` inside `@media print`, top-level `break-before: page`, `break-before: auto` → `[]`. All present.
- ZG-23 debt: `paint.test.mjs` (12) covers `paintStart` hit / missing, `paintOrderReasons` clean / no fcp / no sheet entry / fcp before sheet / fcp before hold, `paintPresenceReasons` clean / missing / unexpected / no rules / UA serif via the injected predicate; `sheet-blocking.test.mjs` (8) covers 1 link, 0, 2, in-body, `rel=preload`, `media`, `onload`, `disabled`. The debt owed since ZG-23 is paid, not re-recorded.
- Runner wiring: `"test:unit": "node --test scripts/probes"`, `"test": "npm run test:unit && npm run probe"`; `Justfile` line 38 `verify` calls `npm test`, so a red unit file stops `just verify` before Playwright launches. No `playwright` import in any `*.test.mjs` (grep empty); `zg-11.test.mjs` pulls `zg-11.mjs`, whose imports are `node:child_process`, `node:fs`, `node:path` and the lib — no browser is launched by the unit run (113 ms total).

Would they fail on a wrong calc — checked for real on a scratch copy under `/tmp/zg11-mut` (product files untouched), four mutants at once: `fitsOnPage` `<=` → `<` killed by `paginateBlocks keeps a block that exactly fits`; `page1Fill` dropping the `first[0].top` subtraction killed by the 0.9 case; `compositeOver` ignoring alpha killed by both composite cases; `bulletMarkerReasons` dropping the inked clause killed by the pale-glyph and pale-bar cases. 6 failures, 73 passes, every mutant caught by a test whose name says which behaviour died. Reasoned for the rest: `countPdfPages` without the `(?!s)` guard would return 3 against the decoy; `forcedBreakReasons` without `stripComments` would return one reason for the comment-only case; `pageIndexOfY` with a strict `<` on the bottom edge would still pass the hit case at y = 25 — a boundary case at `y === block.bottom` is worth adding but the probe never queries an exact edge.

Not covered and acceptable: the Playwright actions (`readAtomicBlocks`, `readEntryHeaderMarks`, `readBulletMarker`, `readInkColors`, `openResumePage`, `printToPdf`) are exercised by the probe run; `describeMarker` / `describeInk` (PASS-line text) and `relativeLuminance` for a non-white colour have no direct case. Follow-ups, not blockers.

BLESS ZG-11 phase-1 — every case the pass-1 verdict listed exists under `node:test` with the standard library only, the ZG-23 debt is paid in the same layout, the unit runner is wired ahead of the probes so `just verify` runs it first, no unit file imports Playwright, and four live mutations of the paginator, fill, compositor, and marker calcs were each caught by a named test.

## Phase 1 — Architect

System-level read only; no code written or edited.

Strata. `lib/contrast.mjs` (pure, no imports) → `lib/print-ink.mjs` (imports contrast; two actions returning records, calcs over the records) and `lib/print-geometry.mjs` (data table, calcs, two actions, one composition) and `lib/pdf.mjs` / `lib/page.mjs` (actions plus one calc each) → `probes/zg-11.mjs` (per-PBI composition over lib, exports `zg11Probes` + the two sheet-source strategies + one calc) → `probes.mjs` (orchestration: imports, reporter, banners, constants, one 20-line `zg11Group`). No lib module imports another PBI's module or the runner; `paint.mjs` and `sheet-blocking.mjs` (ZG-23) sit as peers with no dependency on the ZG-11 modules. Tests are co-located `x.test.mjs` next to `x.mjs`, one per module, discovered by `node --test scripts/probes` with no registry to maintain — a new module's tests appear by naming convention. The runner's pre-existing calcs (`extractMediaBlocks`, `isBlankCanvas`, `missingPrintSections`) are still inline; not this PBI's to move, but the direction is now set and the next touch of them should follow it.

`FORCED_BREAK_SCAN` is the right shape: a per-theme table keyed by theme id with `{ sheet, sectionId }`, so a second theme with a watched section is a row, not a branch; the sheet name is data so the static scan is not tied to the live/git strategy. `PAGE_RULES` is the same pattern for paper geometry. One structural risk to name: `PAGE_RULES` is a second copy of each sheet's `@page` numbers. `article-width` catches width drift live, but a margin change to `@page` height would silently desynchronise `printableHeightPx` and the page-1 ratio; a static check that each row matches the sheet's `@page` text (or deriving the row from it) belongs to whichever PBI next touches `@page`. Not a phase-1 blocker: the PBI dictates the numbers.

Fixture lock: `FIXTURES: &[FixtureRow]` with one `locked_html` is exactly what ZG-19 needs — a row per sample, `every_fixture_row_is_crate_output` locks all rows, and per-fixture count tests can be added beside `long_resume_html_is_crate_output`. The re-lock in the count test (pass-1 nit 5) will multiply per ZG-19 row; splitting a `rendered(row)` calc out of `locked_html` before ZG-19 appends is the cheap fix.

Nothing ZG-12 / ZG-13 must undo. ZG-12 gets `openResumePage(browser, { origin, theme, width, height, fixtureHtml })` from `lib/page.mjs` — `origin` is an explicit parameter rather than the PBI's implicit runner global, which is the correct boundary (the lib does not know the server). ZG-13 does not touch `U3_PRINT_PAGES` / `LONG_PRINT_PAGES`, which stay in the runner beside each other as the PBI specifies. The expected-fail reporter is confined to `zg11Reporter()` and a single constant; phase 2 deletes 15 lines and nothing else moves. `gitSheets` shelling to `git show` lives in the per-PBI module, not lib — right, since it is ZG-11's anti-vacuity device; if ZG-12 needs the same, it is promoted then, not now.

BLESS ZG-11 phase-1 — the lib is stratified pure → actions → per-PBI composition → runner with tests co-located per module, the two theme-keyed tables (`FORCED_BREAK_SCAN`, `PAGE_RULES`) replace branches with rows, the cargo lock is a table ZG-19 appends to, and ZG-12 / ZG-13 consume `lib/page.mjs` and the runner constants as they stand without reversing anything; the `@page`-duplication risk and the ZG-19 re-lock split are recorded as follow-ups.
