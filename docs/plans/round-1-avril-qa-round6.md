# AVRIL round 1 — QA Architect review (cycle 6)

**Role:** `qa-architect-agent` (second adversary) · **Date:** 2026-08-23 · **Cycle:** 6 (fresh review after the Generator's cycle-5 revise and the PO's cycle-6 BLESS)
**Scope:** ZG-11, ZG-12, ZG-20 — my cycle-5 REJECTs, revised in `round-1-avril-generator-round5.md`. Prior verdicts lapsed with the body edits; each item is judged from scratch — every cycle-5 blocker is checked against the **current body** (not the Generator's change log), then the whole body is read again as if for the first time, and every example in every AC is run against its stated rule by hand. ZG-19 and ZG-21 are being revised concurrently and are not touched. Nothing else is judged.
**Inputs read in full:** `docs/plans/round-1-avril-qa-round5.md` (my blockers and set-level notes), `round-1-avril-qa-round1.md` (conventions), `round-1-avril-generator-round5.md`, `round-1-avril-po-round6.md`, `round-1-avril-intent.md`, the three current bodies (`pinto show ZG-11 ZG-12 ZG-20 --plain` and `pinto list --json`: `updated` 2026-08-24T01:24:18Z on all three, edges unchanged — ZG-12 → ZG-11, ZG-20 → ZG-19), `themes/{quarto,switchyard,nightgarden}.css` with line numbers, `frontend/scripts/probes.mjs`, `frontend/package.json`, `justfile`, `skeleton/example.html`, `docs/persona-rounds/round-0/devon.md`.

Mandate: *BLESS only PBIs whose acceptance criteria are complete, falsifiable, and hostile to happy-path theater; otherwise REJECT with the missing cases.*

I judge testability only. I do not author bodies, touch the board, or reopen product scope. Conventions from cycle 1 apply unchanged: one owner per shared fixture or helper; probe lines are `PASS  ZG-n/<slug>` / `FAIL  ZG-n/<slug> <reason>`; a PBI's own AC never prints `SKIPPED`; only Chromium is installed; nothing in the repo rasterises a PDF; `U3_PRINT_PAGES` is asserted with `!==`; `just verify` stays offline.

## Facts I verified before judging (they change verdicts)

| Fact | How | Affects |
| --- | --- | --- |
| Every line number the three bodies cite is right today: Quarto `@page :first` 58, `list-style: none` 148, `.rz-section { break-inside: avoid-page; page-break-inside: avoid }` 174–178 (top level), `.rz-bullet::before { content: "– " }` 268–269, print block 334; Switchyard `list-style: none` 115, `.rz-entry-header > *` 121, screen bar 256–257 (inside `@media screen`, 129), `@media print` 334, `.rz-section` avoid 397–398, `.rz-section--projects { break-before: page; page-break-before: always }` 402–403; Nightgarden sheen 173, `list-style: none` 199, `.rz-section` rise 225, foxfire 271, `.rz-entry-header > *` 274, dot 309–310, `@supports` 364, guard 390, `html`/`body` background `#070b14` (100–116) | `grep -n` | ZG-11 scope-in, `bullets-print`, `no-forced-break`; ZG-12 scope-in; ZG-20 AC 1 |
| **In Chromium print emulation, Quarto's rule computes `breakInside` = `avoid`, not `avoid-page`**: `page-break-inside: avoid` (line 177) is an alias of `break-inside: avoid` declared later in the same block and wins the cascade. Switchyard's `.rz-section--projects` computes `breakBefore` = `page`. | `/tmp/persona-lab/qa6-facts.mjs` (Playwright Chromium 151, `emulateMedia({ media: 'print' })`) | ZG-11 `no-forced-break` anti-vacuity parenthetical |
| **A `::before` box has no `getBoundingClientRect()`.** Pseudo-elements are not DOM nodes; `Element.prototype.pseudo` exists in this Chromium but `el.pseudo('::before')` returns `undefined` for a generated box, so there is no object to call a rect method on. The executable read is `getComputedStyle(el, '::before').width` / `.height`: a sized empty-content bar reports `8.79688px` / `2px` and `backgroundColor` `rgb(180, 83, 9)`; `content: none` reports `width` `auto`. Computed `content` is `""` (two quote characters) for an empty string and `"– "` for Quarto's glyph; `li` `listStyleType` is `none` under `list-style: none`. | same script | ZG-11 `bullets-print` marker-exists clause |
| **`staticProbes()` prints ten `PASS  S1`/`S2`/`S3`/`S4`/`S5`/`U3` lines before the server starts** — lines 238, 244, 249, 259, 264, 270, 280, 290, 302, 315 of `probes.mjs` — e.g. `PASS  S1 ports.js keeps the outgoing Theme sheet until the incoming sheet is ready`, `PASS  U3 Garden Print still prints the child document via contentWindow.print()`. | `PROBE_PORT=4399 node scripts/probes.mjs` killed after 15 s; output captured | ZG-20 `--static` AC |
| `staticProbes()` runs first (line 1112), then `startServer()` + `browserProbes()`, `process.exit(1)` on failures (1121–1123), success line `All RZ-3, RZ-S1…S5, and U3 print probes passed.` (1126); no flag parsing; `chromium.launch({ headless: true })` (1092). `npm test` = `npm run probe` = `npm run build && node scripts/probes.mjs`; `just test-frontend` = `PROBE_PORT=4310 npm test`; `just verify` = `fmt clippy test`. Node v20.18.0. | read; `node --version` | ZG-20 wiring and `--static` |
| ZG-20's `content` examples run against the stated regexes (`/\p{L}{3,}/u`, `/[\p{Script=Han}\p{Script=Hiragana}\p{Script=Katakana}\p{Script=Hangul}]{2,}/u`) in Node 20: `"Jordan Hale"` 1, `"名前"` 1 (U+540D U+524D), `"氏"` 0, `"Jr"` 0, `"vs"` 0, `"$ "` 0, `"▌"` 0, `" · "` 0, `"– "` 0, `"Page"` 1, `"Page "` 1, `"이름"` 1; every first-party `content:` string (`""`, `" "`, `", "`, `": "`, `" · "`, `"– "`) 0. Rule and examples agree. | `node -e` | ZG-20 content AC; Notes line |
| Nightgarden's three `@keyframes` (68, 80, 90) use `from` / `to` keyframe selectors; no first-party sheet uses a percentage keyframe selector. Devon's Cathode has an `rz-blink` keyframe animation (`docs/persona-rounds/round-0/devon.md` row 3). | grep | ZG-20 selector rule vs `@keyframes` bodies |
| `skeleton/example.html`: twelve sections `rz-summary` … `rz-projects` (projects last), one `rz-is-current` entry, `.rz-identity` present. | grep | ZG-12 `painted-with-support` guard, sheen/foxfire scope line |
| `Element.getAnimations()` without `{ subtree: true }` returns only the element's own (and its pseudo-elements') animations; a finished CSS animation with `fill-mode: both` is still "in effect" and is returned. | Web Animations spec; consistent with the AC's pre-change claim | ZG-12 `painted-without-support` scoping and anti-vacuity |
| `frontend/dist/` exists on this machine (built); `pinto list --json` shows no body outside the three in scope written after cycle 5 except ZG-19/ZG-21 (concurrent, out of scope). | `ls`, JSON | evidence runs are possible; scope discipline |

---

## ZG-11 — Print a real résumé without blank pages, lost bullets, or pale ink

### Cycle-5 blockers, checked against the current body

| Cycle-5 blocker / note | Current body | Resolved |
| --- | --- | --- |
| `bullets-print` marker-exists clause failed by construction on an empty-content bar (`content` serialises to `""`, `list-style-type` is `none` in all three themes) | Three alternatives: a quoted string other than `""`; **or** `content` `""` (named as the two-character serialisation) / `none` / `normal` **and** the `::before` box has `getBoundingClientRect()` `width` > 0 and `height` > 0; **or** `list-style-type` not `none` (flagged as never how a first-party theme passes, with the three line numbers). Ink clause unchanged. | the logic is fixed; the **read** it names does not exist — below |
| `no-forced-break` grepped `extractMediaBlocks` output and could not see Quarto's top-level rule | Replaced by computed `breakInside` `auto` on every `.rz-section` in print emulation on the top-level page for all three themes, `breakBefore` other than `page` on Switchyard's `.rz-section--projects`, a whole-sheet static scan of `switchyard.css` (explicitly not `extractMediaBlocks`), and a pre-change Quarto FAIL. Scope-in states where each sheet's rule lives (verified: 174–178 top level / 397, 402 inside print / none). | yes — the assertion is falsifiable on Quarto; one token in the anti-vacuity parenthetical is wrong, below |
| Non-blocking: fixture ranges not a ceiling | "the ranges above are a guide, not a ceiling … exceed the highlight ranges … until it does; the AC decides, not the counts" | yes |
| Non-blocking: oversize atomic block in `paginate` | "an atomic block taller than the printable height starts a new simulated page and is placed alone on it (the next block begins the following page)" | yes |
| Non-blocking: `--hide-scrollbars` | Helper uses the suite's `chromium.launch({ headless: true })`, never passes `ignoreDefaultArgs`; an `article-width` FAIL is read as a UA margin or print-block regression | yes |

### Fresh review

I re-read every AC against the sheets and ran the two facts the revised ACs rest on in Chromium. `article-width`, `fixture-trips-e1`, `page1-fill` (with its pre-change < 0.85, which I re-derived: Quarto's top-level avoid makes `#rz-experience` one atomic block > 952 px, so it is placed alone on page 2 and page 1 holds the summary only), `page-count`, `entry-intact`, `print-ink` (`.rz-entry-secondary` keeps `var(--rz-iris)` `#c7b4ff` in print today — the probe fails pre-change on exactly Marcus's pale lilac, and `#4b5563` on the other three already passes), the BAR-T1/U3 guards and the cargo lock are exact and each has a way to fail. `no-forced-break` is now a real Quarto oracle: today's sheet computes `avoid` on every `.rz-section` in print emulation, the assertion is `auto`, so it FAILs pre-change and can only pass by editing or overriding line 174–178. Two defects remain, one of which stops a tester from executing the AC as written:

1. **`bullets-print`'s marker-exists clause names a read that does not exist.** The second alternative requires "the `::before` box has `getBoundingClientRect()` `width` > 0 and `height` > 0". A pseudo-element is not an Element; there is no node to call `getBoundingClientRect()` on, Playwright locators cannot select `::before`, and in this Chromium `el.pseudo('::before')` returns `undefined` (verified). The clause the whole Switchyard / Nightgarden bar path depends on — the body itself says "a Switchyard or Nightgarden bar satisfies the marker-exists clause through its sized box, not through `content`" — cannot be evaluated by the method the AC states. The ink clause in the same AC is phrased as computed style (`width` > 0, `height` > 0, `background-color`, `print-color-adjust` "computed on it"), which is executable: `getComputedStyle(el, '::before').width` / `.height` return the used box size (`8.79688px` / `2px` for a Switchyard-shaped bar) and `auto` when `content: none` generates no box (verified). Required: replace `getBoundingClientRect()` in the marker-exists clause with the computed-style read (`getComputedStyle(el, '::before').width` and `.height` parse to > 0 px; `auto` fails), so both clauses use one read. One token.
2. **`no-forced-break`'s anti-vacuity names the wrong computed value, and the AC says that line is the evidence.** The parenthetical says the pre-change Quarto sheet "computes `avoid-page` in print emulation — that FAIL line is recorded in evidence". In Chromium the rule at 174–178 computes `breakInside` = `avoid`: line 177's `page-break-inside: avoid` is an alias of `break-inside: avoid`, is declared after line 176, and wins (verified). The assertion (`auto`) still fails, so the probe is not vacuous — but the evidence line the AC demands will read `avoid`, and a reviewer checking evidence against the AC text sees a mismatch. Required: say "computes `avoid` (line 177's `page-break-inside: avoid` alias is declared later and wins) — anything other than `auto` fails", so the recorded FAIL line matches the AC. This is the same rule-vs-example defect class as cycle 5; fold it in with blocker 1.

Non-blocking (fold in without re-review):

- **`.rz-header` is not in `paginate`'s block list.** The atomic/leaf definition names `.rz-entry`, `.rz-prose p`, `.rz-skill-group`, `.rz-section-title`; the header (`break-after: avoid`, not `break-inside: avoid`) is never placed, so the simulated page 1 has the header's height (~100–130 px) more capacity than the real page 1. Pre- and post-change ratios are affected equally and `page-count` checks the real PDF, so the verdicts stand; say "the `.rz-header` is the first block on page 1" so the ratio does not overstate capacity and two implementations agree.
- **`bullets-print` on Switchyard pre-change.** Today the bar lives only in `@media screen` (256–264); in print emulation `content` is `none` and `width` is `auto`, so the pre-change sheet fails the marker-exists clause (E2 reproduced); Nightgarden pre-change has a box (`0.4rem`, mint) and fails the ink clause. Worth one sentence in evidence so both failure modes are seen once.

**REJECT ZG-11 — `bullets-print`'s marker-exists clause requires `getBoundingClientRect()` on a `::before` box, which has no such method (pseudo-elements are not Elements; `el.pseudo('::before')` is `undefined` in this Chromium), so the clause every bar-marker implementation must pass cannot be executed as written — use `getComputedStyle(el, '::before').width`/`.height` (verified `8.79688px`/`2px` for a bar, `auto` for no box); and `no-forced-break`'s named evidence line says Quarto computes `avoid-page` when Chromium computes `avoid` (line 177's alias wins — verified), so the recorded FAIL will not match the AC text. Both cycle-5 blockers are logically closed; these are the two reads that make them runnable.**

## ZG-12 — Fit long names in Nightgarden, paint sections without scrolling, keep bullet line breaks

### Cycle-5 blocker, checked against the current body

| Cycle-5 blocker / note | Current body | Resolved |
| --- | --- | --- |
| `painted-without-support` demanded `document.getAnimations()` empty; the infinite sheen (173) and foxfire (271) make that unsatisfiable on a correct sheet | `[...document.querySelectorAll('.rz-section')].flatMap(s => s.getAnimations())` is empty — "equivalently `document.getAnimations().some(a => a.animationName === 'rz-rise')` is `false`"; the sheen and foxfire "keep running and are outside the assertion"; pre-change claim restated precisely (stripped remainder still carries line 225's fill-`both` rise, so the sections report the finished animation — verified: a finished fill-`both` animation is still "in effect" and is returned by `getAnimations()`). `reduced-motion` uses the identical expression. New scope-in line records the two animations verbatim with their line numbers (verified 173 / 271, top level, infinite) and the rule that every "nothing animates" oracle is `.rz-section`-scoped | yes |
| Non-blocking: screenshot may trigger the `view()` entry | Computed `opacity` named the primary oracle; pixel count is the paint confirmation; `#070b14` body background (verified: `html`/`body` are `#070b14`, `#0c1220` is `.rz-resume`) | yes |
| Non-blocking: `pre-line` page | "on the top-level page via `openResumePage({ theme, width: 1280, height: 800 })` for each first-party theme" | yes |

### Fresh review

I checked the one thing that could make the scoped oracle leak: `Element.getAnimations()` without `{ subtree: true }` returns the element's own animations and those of its pseudo-elements, not its descendants — so the foxfire on a `.rz-entry.rz-is-current` inside a section is not in the `.rz-section` list, and the assertion is satisfiable post-change exactly when no `.rz-section` animation exists outside the stripped `@supports` block. Post-change the remainder has no `.rz-section` animation at all (scope: "Outside that block `.rz-section` has no `animation`"), so the list is empty and both `opacity` reads are `1`; pre-change the list holds twelve finished fill-`both` `rz-rise` animations, so the probe fails on the one clause that distinguishes the two sheets. `painted-with-support` keeps the deterministic pre-change `opacity` `0` (fill `both` applies the `from` keyframe before the range) and the post-change `1` (fill `forwards`, no before-phase effect), with the guard that both sections are below the 800 px fold — twelve sections, projects last, satisfiable. `reduced-motion`: the line-390 guard sets `animation: none !important` on `.rz-section`, so the same expression is empty and `opacity` is `1`. `rise-css-structure` is unchanged from the version I blessed in cycle 5 and still FAILs pre-change on (a) at line 225. `name-fits` (`.rz-identity` exists; 390×844 collapses to one column at ≤ 48rem) and `pre-line` (sibling ratio, no `line-height` dependence) are exact. The dependency on ZG-11 is real (`openResumePage` signature matches ZG-11's body) and the edge exists. A stub that deletes the rise fails BAR-T1 and `rise-css-structure` (b); a stub that keeps fill `both` fails `painted-with-support` pre-/post-change alike; a stub that only strips the animation outside `@supports` but leaves `opacity: 0` on `.rz-section` fails (c) and `painted-without-support`.

Non-blocking:

- `painted-with-support`'s pixel decode reads the `body` background; say the probe reads `getComputedStyle(document.body).backgroundColor` rather than the literal `#070b14`, so a future palette change does not silently shift the threshold. Verdict unaffected today.

**BLESS ZG-12 — the cycle-5 blocker is closed with a `.rz-section`-scoped `getAnimations()` oracle that I verified excludes the descendant foxfire and the sibling sheen, is empty post-change, and returns the finished fill-`both` rise pre-change; every other AC is a computed-`opacity` read, a static structure check with a named pre-change FAIL, or a geometry inequality, and a superficial stub fails at least three of them.**

## ZG-20 — Ship `npm run lint-theme` so a Designer knows pass or fail before opening a PR

### Cycle-5 blocker, checked against the current body

| Cycle-5 blocker / note | Current body | Resolved |
| --- | --- | --- |
| `content:"名前"` → `content` contradicted `\p{L}{3,}` (two code points) | Rule gains a second clause: a run of 2+ characters from `[\p{Script=Han}\p{Script=Hiragana}\p{Script=Katakana}\p{Script=Hangul}]{2,}`, `u` flag, Node 20 native; unit list gains `"氏"`, `"Jr"`, `"vs"` → no error and annotates `"名前"` as the script clause. I ran all thirteen content examples and every first-party `content:` string against both regexes in Node 20: every one matches its stated outcome | yes |
| Non-blocking: meta-test cost and wiring | `probes.mjs --static` owned here (stop after `staticProbes()`, no server, no Chromium, same lines and summary, exit contract, unchanged without the flag); `npm test` becomes `npm run probe && node --test scripts/*.test.mjs`; the meta-test spawns `--static`; a new AC pins the flag's behaviour | wired — but the new AC's negative is wrong, below |
| Non-blocking: `@page :first{}`, `::selection`, `::marker` unit cases | present | yes |

### Fresh review

The rule set decides every construct in the three first-party sheets, the fixture is five stated lines with three named errors at named line numbers, the exit-code contract has both branches, the meta-test asserts the lint's own named `FAIL  ZG-20/lint zz-bad.css:{3,4,5}` lines and the exact post-removal count, and the `content` rule now agrees with its examples (verified by execution, not by reading). I confirm the rule, including the two-character CJK-script clause: it is the operationalisation of "résumé words never only in `content:`" that flags a two-character CJK label and a three-letter Latin word while letting `"Jr"`, `"vs"`, a single CJK glyph and every BAR-X2 separator through; the Notes line may drain on the next pass. Two ACs do not survive contact with `probes.mjs` and the sheets:

1. **The `--static` AC's negative fails on a correct implementation.** It requires that `node scripts/probes.mjs --static` "prints no browser-suite line (no `PASS  S1`…`S5`/`U3` line, no `All RZ-3` summary)". `staticProbes()` — the half that `--static` is defined to run — itself prints ten such lines before any server or browser exists: `PASS  S1 ports.js keeps the outgoing Theme sheet …` (238), `PASS  U3 Garden Print still prints …` (244), `PASS  U3 File → Print …` (249), `PASS  S3 Nightgarden is rz-target:web …` (259), `PASS  S3 Nightgarden print no longer …` (264), `PASS  S2 Quarto is rz-target:print …` (270), `PASS  S5 unknown/empty Theme query …` (280), `PASS  S4 theme options are buttons …` (290), `PASS  U3 @media print drops …` (302), `PASS  U3 narrow .garden-frame vh …` (315). I ran the suite and captured them. A tester who reads the parenthetical as written FAILs a correct `--static`; one who reads "browser-suite line" as intent passes it — two testers, two verdicts, the defect class I rejected ZG-17 and ZG-20 for. Required: name lines that only the browser half prints — e.g. no `Live sandbox has data-rz-schema` / `Jordan Hale is in .rz-name` line (476–483), no `S2 <id>:` / `U3 <id>:` per-theme line, no `printToPDF` in any line, no `All RZ-3` summary — and, since the static half prints its own `S1`…`U3` lines, say so, so the meta-test's "exactly three `PASS  ZG-20/lint` lines" count is not confused with them. Also state what `--static` prints on success (the AC forbids `All RZ-3…` but names no replacement; "nothing after the last probe line" is enough).
2. **The selector rule, as written, classifies keyframe selectors as errors, and AC 1 requires the opposite.** The rule says "a selector whose compounds are only `*`/type/pseudo is an error" and nothing about `@keyframes` bodies; the lint is declared a "regex/tokeniser over declarations", not a parser. Nightgarden's three `@keyframes` (68–99) contain `from {` and `to {` — type-only compounds by the rule — so a tokeniser that follows the rule fails AC 1 (`nightgarden.css` exits 0), and one that passes AC 1 does so by an exclusion the rule never states. No unit case pins keyframe selectors at all, and percentage selectors (`0%`, `50%`) — which a Designer's blink or flicker animation uses (Devon's Cathode has `rz-blink`) and which no first-party sheet contains — are undefined by every AC, so a lint that special-cases `from`/`to` and errors on `50%` passes this PBI and sends Devon a false FAIL. ZG-12's `rise-css-structure` already states "`@keyframes` bodies excluded from the selector scan"; this PBI must say the same. Required: add to the selector rule "the selector rule does not apply inside `@keyframes` bodies (`from`, `to`, `<percentage>` are keyframe selectors)" and one unit case `@keyframes x{from{opacity:0}50%{opacity:.5}to{opacity:1}}` → no `selector` error (and, since `@keyframes` is present without reduced-motion in that snippet, warning `motion` — or include the guard).

Non-blocking (fold in with the fix):

- **`node --test` runs test files concurrently** in Node 20 when given several files. The meta-test creates `themes/zz-bad.css` while ZG-19's `blank-theme.test.mjs` and ZG-10's `page-estimate.test.mjs` may be running; today neither reads `themes/` as a directory, so nothing breaks, but say "the meta-test is the only test that writes into `themes/`, and it removes the file in `finally`" so a future test that lists `themes/` knows the constraint. Alternatively `--test-concurrency=1` in the `test` script.
- **`CONTRIBUTING.md` ownership** is ZG-18's (PO's note, AXEL ordering). Testability is unaffected: the grep hits whichever item writes the sentence.

**REJECT ZG-20 — the new `--static` AC forbids any `PASS  S1`…`S5`/`U3` line, but `staticProbes()` itself prints ten of them (lines 238–315; captured by running the suite), so a correct `--static` fails the AC as written; and the selector rule, applied as stated to `@keyframes` bodies, makes Nightgarden's `from`/`to` (lines 68–99) `selector` errors while AC 1 requires exit 0, with no unit case for keyframe selectors and none for `<percentage>` ones a Designer writes. Name browser-only lines for the `--static` negative, exclude `@keyframes` bodies from the selector rule with a unit case. The cycle-5 blocker is closed: every `content` example agrees with the rule under execution, and I confirm the rule including the CJK clause.**

---

## Summary

| id | PO cycle 6 | QA verdict | one-line reason |
| --- | --- | --- | --- |
| ZG-11 | BLESS | **REJECT** | `bullets-print` demands `getBoundingClientRect()` on a `::before` box (no such method; `el.pseudo('::before')` is `undefined` — verified) — use `getComputedStyle(el,'::before').width`/`.height`; `no-forced-break`'s named evidence line says `avoid-page` where Chromium computes `avoid` (alias at 177 wins — verified). Both cycle-5 blockers logically closed |
| ZG-12 | BLESS | **BLESS** | `.rz-section`-scoped `getAnimations()` excludes the sheen and foxfire, is empty post-change and non-empty pre-change; computed `opacity` is the primary oracle everywhere; every AC has a named way to fail |
| ZG-20 | BLESS | **REJECT** | `--static` AC forbids `PASS  S1`…`U3` lines that `staticProbes()` itself prints (ten, lines 238–315, captured); selector rule as stated errors on `from`/`to` inside `@keyframes` while AC 1 requires Nightgarden exit 0; no keyframe-selector unit case. CJK `content` rule confirmed by execution |

1 BLESS, 2 REJECT. ZG-12 proceeds to the Visionary CTO. ZG-11 and ZG-20 return to the Generator for the deltas above and then need the full PO → QA → CTO chain again.

## Set-level notes for the Generator

- **Pseudo-elements have no rect.** Any AC that measures a `::before`/`::after` box must read `getComputedStyle(el, '::before')` (`width`, `height`, `backgroundColor`, `content`); `getBoundingClientRect()`, Playwright locators and `el.pseudo()` do not reach generated boxes. ZG-11 is the instance this cycle.
- **`page-break-*` aliases win when declared later.** Quarto 176–177, Switchyard 397–398 / 402–403 / 422–423, Nightgarden 500–501 / 505–506 all pair a `break-*` with its legacy alias on the next line; the computed value is the alias's (`avoid`, `page`). Anti-vacuity parentheticals must name the value Chromium reports, or say "anything other than `auto`".
- **`staticProbes()` is not silent about S1–S5/U3.** Its ten `PASS  S…`/`U3 …` lines are static file checks, not browser results. Any "the browser suite did not run" negative must name browser-only lines (`Live sandbox …`, `S2 <id>:`, `U3 <id>:`, `printToPDF`, `All RZ-3`).
- **A regex lint's selector rule must say where it does not apply.** `@keyframes` bodies (`from`/`to`/`<percentage>`), `@page` preludes and `@property` names all look like selectors to a tokeniser; ZG-12's own probe already excludes keyframes bodies, ZG-20 pins `@page :first` and must pin keyframes the same way.
- **Run the example against the rule, and the AC against the source.** Cycle 5's defect class (rule vs. example) is closed in all three bodies; cycle 6's is AC vs. runtime (a non-existent method, a wrong computed value, lines the AC says are absent but the source prints). Before submitting, execute each new oracle once against today's tree.
- **Unchanged owners and edges are consistent.** `openResumePage` / `printableWidthPx` / `LONG_PRINT_PAGES` (ZG-11) → ZG-12; `--static` and the `npm test` glob (ZG-20) with no other editor of `package.json` `test` (confirmed against ZG-10 and ZG-19 bodies via the PO's check). No cycle in the graph.
